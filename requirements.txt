# Python 3.10+ recommended. Pin versions locally if you need reproducible installs.
eel>=0.16.0
SpeechRecognition>=3.10.0
requests>=2.31.0
pywhatkit>=5.0
playsound>=1.3.0
# Microphone input — platform-specific wheels may require extra setup on Linux/macOS.
PyAudio>=0.2.14
